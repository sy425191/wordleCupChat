const WordleInputBox = () => {
  return (
    <input
      type="text"
      className="w-full h-full border-2 outline-none border-slate-500 rounded bg-slate-100 text-xl font-bold text-center uppercase"
      maxLength={1}
    />
  );
};

export default WordleInputBox;
